#include <stdio.h>
#include <stdlib.h>

//int scanf(const char *format, );
int x;
int main() {
    scanf("%d", &x);
    // -> devolve informações sobre o que foi lido com sucesso.
    // O que importante é a quantidade de % 
    return 0;
}
