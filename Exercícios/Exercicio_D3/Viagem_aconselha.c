#include <stdio.h>

int main() {
    int dia_viagem, R, Ri, i, codigo, nota, maior_nota, maior_codigo rests[200];
    int i_restaurantes;
    
    while (scanf("%d", &R) != EOF && R != '\n') {
        int i = 0;
        rests[i]=R;
        for(i=  1; i == 2*rests[0]; i++){
            if (i % 2 == 0) {

            }
            i_restaurantes = 0;
            if(i % 2 == 0) {
                scanf("%d", nota);
                notas[i_restaurantes] = nota;
            } else {
                scanf("%d", codigo);
                codigos[i_restaurantes] = codigo;
            }

        i_restaurantes++

    }
        i++;
    }
    return 0;
}