#include <stdio.h>

int ultrapassa(int *vetor, int limite) {
    
    for (int i = 0; i < vetor; i++) {

    }
}

int main() {
    int sequencia_v[], i = 0;
    int limite;
    while (scanf("%d", &sequencia_v[i]) != 0) {
        i++;
    }
    scanf("%d", limite);


    return 0;
}