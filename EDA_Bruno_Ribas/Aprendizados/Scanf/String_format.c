#include <stdio.h>
#include <stdlib.h>

// int scanf(const char *format, ...);
// String format vai meio que falar o que o scanf deve ler. 
int main() {
    //String de formato, o primeiro argumento do scanf();
    //White Spaces, podemos colocar espaços em branco, tab, quebra de linha, etc.
    //scanf(" "); - Qualquer quantidade de espaço
    scanf("abc");
    //scanf("a b c") = scanf("abc")
    
    return 0;
}
