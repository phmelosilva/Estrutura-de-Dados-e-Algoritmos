#include <stdio.h>

int main() {
    int num;
//    while (scanf("%x", num) != EOF) {
//         printf("%d", num);
//     } 
    while (scanf("%x", &num) != EOF) {
 
    }
    scanf("%x", &num);
    for
    printf("%x\n", num);
    printf("%d\n", num);
    printf("%c\n", num);

    return 0;
}