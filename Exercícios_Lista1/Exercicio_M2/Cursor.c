#include <stdio.h>

int main() 
{
    int linhas, cursor_linha, cursor_col;
    char texto[1001], comando_cursor;

    scanf("%d\n", linhas);
    for (int i=0; i < linhas; i++) {
        scanf("%s\n", texto);
        scanf("%d %d", cursor_linha, cursor_col);
        
    }
    return 0;
}